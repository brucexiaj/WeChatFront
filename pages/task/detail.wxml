<view class="orderGoodsListBox">
	<view>
		<view class="taskBarList" data-detailid="{{taskDaily.id}}">
			<view class="timeType">
				<view class="title">{{taskDaily.taskTitle}}</view>
				<view class="time">{{taskDaily.taskStartTimeStr}}</view>
			</view>
			<view class="status">
				<view class="statusDes">{{taskDaily.statusDes}}</view>
				<view class="scheduleBox">
					<view class="{{taskDaily.statusValue}}" style="width:{{taskDaily.schedule}}%"></view>
				</view>
			</view>
			<view class="purchaseNum">
			{{taskDaily.allInCount}}/{{taskDaily.allCount}}
			</view>
			<view class="assigner" >
				{{taskDaily.ownerName}}
			</view>
		</view>
		<view class="taskListBox">
			<view class="taskList">
				<image src="{{item.mainPic}}" class="main" />
				<view class="info">
					<view class="name">{{item.name}}</view>
					<view class="upc {{upc!='' ? upc != itemSku.upc ?'error':'true' : ''}}">upc: {{itemSku.upc}}</view>
					<view class="sku">{{itemSku.color}}<block wx:if="{{itemSku.scale}}">+{{itemSku.scale}}</block></view>
					<view class="price" wx:if="{{taskDailyDetail.taskMaxPrice}}">采购价 <text>{{char_lt}}= ${{taskDailyDetail.taskMaxPrice}}</text></view>
				</view>		
				<view class="editBtn">
					<image src="../../images/icon_calc.png" mode="widthFix"/>
					<text>{{taskDailyDetail.count - taskReceipt.transQuantity - taskReceipt.quantity}}</text>
				</view>
			</view>
		</view>
	</view>
	<view class="listBox">
		<label>UPC：</label>
		<input placeholder="{{upcPlaceholder}}" maxlength="20" bindinput="upcInput" value="{{upc}}" placeholder-class="placeholder"/>
		<image src="../../images/icon_upcScan.png" class="scan" mode="widthFix" bindtap="scan"/>
	</view>
	<view class="listBox">
		<label>原价：</label><label>${{itemSku.costPrice}}</label>
		<label style="width: 60px;margin-left: 50px;">采购价：</label><label style="width: auto;">$</label>
		<input maxlength="8" style="width:100px;" type="digit" placeholder="请输入采购价"  bindinput="purchasePriceInput" value="{{price}}" placeholder-class="placeholder"/>
	</view>
	<view class="listBox">
		<view style="width:50%;float:left">
			<label>线下：</label>
			<input style="width:100px" type="number" maxlength="5" placeholder=""  bindinput="quantityInput" value="{{quantity}}" placeholder-class="placeholder"/>
		</view>
		<view style="width:50%;float:left">
			<label>在途：</label>
			<input style="width:100px" type="number" maxlength="5" placeholder=""  bindinput="transQuantityInput" value="{{transQuantity}}" placeholder-class="placeholder"/>
		</view>
	</view>
	<view class="listBox">
		<label style="width:75px">采购地点：</label>
		<input placeholder="采购地点" maxlength="20" placeholder=""  bindinput="addInput" value="{{buySite}}" placeholder-class="placeholder"/>
		<image src="../../images/location.png" class="location" mode="widthFix" bindtap="choseAddress"/>
	</view>
</view>
<view class="footerBox">
	<view class="price">
		付款额
		<text>${{totalPrice}}</text>
	</view>
	<view class="calcBtn" bindtap="calc">
		结算
	</view>
</view>